using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreatePeoplesList : MainStructure
{
    public CreatePeoplesList(int index,string name, string job, int age, float size, string nationality) 
        : base(index,name, job, age, size, nationality)
    {
    }
}
